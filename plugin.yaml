name: gbrain
version: 1.1.0
description: "GBrain — persistent knowledge brain with brain-agent-loop (auto-detect entities, read brain before responding, write timeline after turns)."
requires_env:
  - MCP_GBRAIN_API_KEY
optional_env:
  - MCP_GBRAIN_URL
  - MCP_GBRAIN_TIMEOUT
hooks: []
